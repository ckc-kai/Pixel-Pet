# Progress Log

Reverse-chronological session log. Newest entry at the top.
See `CLAUDE.md` §8 for format and how detail scales with importance.

---

## 2026-05-18 — main (repo governance)
**Scope:** Added `CLAUDE.md`, `progress.md`, `.gitignore`. No code yet.
**Next:** Plan `docs/architecture.md` (crate layout, IPC, state machine, persistence, activity detection) before any scaffolding.
